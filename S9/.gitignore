# ── secrets ──
.env
.env.*
!**/.env.example

# ── python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# ── runtime state & artifacts (regenerated by running the agent) ──
S9SharedCode/code/state/
S9SharedCode/logs/
S9SharedCode/code/usage.json

# ── gateway ledgers (recreated on first run) ──
*.db

# ── OS noise ──
.DS_Store
